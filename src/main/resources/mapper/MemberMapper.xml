<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pettrainer.pro.member.mapper.MemberMapper">

    <resultMap id="memberResultMap" type="com.pettrainer.pro.member.entity.Member">
        <id     property="memberId"  column="member_id"/>
        <result property="email"     column="email"/>
        <result property="password"  column="password"/>
        <result property="name"      column="name"/>
        <result property="role"      column="role"/>
        <result property="phone"     column="phone"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="memberId">
        INSERT INTO member (email, password, name, role, phone)
        VALUES (#{email}, #{password}, #{name}, #{role}, #{phone})
    </insert>

    <select id="findByEmail" resultMap="memberResultMap">
        SELECT * FROM member WHERE email = #{email}
    </select>

    <select id="findById" resultMap="memberResultMap">
        SELECT * FROM member WHERE member_id = #{memberId}
    </select>

    <select id="findAll" resultMap="memberResultMap">
        SELECT * FROM member ORDER BY created_at DESC
    </select>

    <update id="updateRole">
        UPDATE member SET role = #{role} WHERE member_id = #{memberId}
    </update>

</mapper>
